module.exports = {
    arrowParens: 'avoid', // Use 'avoid' or 'always'
    bracketSameLine: false, // Put > of multi-line JSX elements on a new line
    bracketSpacing: true, // Print spaces between brackets in object literals
    singleQuote: true, // Use single quotes instead of double quotes
    trailingComma: 'es5', // Trailing commas where valid in ES5 (objects, arrays, etc.)
    semi: true, // Print semicolons at the ends of statements
    tabWidth: 4, // Specify the number of spaces per indentation-level
    useTabs: false, // Indent lines with spaces instead of tabs
    printWidth: 120, // Specify the line length that the printer will wrap on
};
